# Copy to .env and fill in your values (.env is gitignored).

# OpenWeather — https://openweathermap.org/api (Current Weather API)
OPENWEATHER_API_KEY=

# Flask session signing (homework4); use a long random string in production
SECRET_KEY=
